version: '1'
services:
  main:
    image: main-sbt
    working_dir: /main
    ports:
      - 127.0.0.1:0001
    depends_on:
      - player
    stdin_open: true
  player:
    image: player-sbt
    working_dir: /player
    ports:
      - 127.0.0.1:0002
    stdin_open: true
